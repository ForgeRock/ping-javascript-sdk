# protect

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build protect` to build the library.

## Running unit tests

Run `nx test protect` to execute the unit tests via [Vitest](https://vitest.dev/).
